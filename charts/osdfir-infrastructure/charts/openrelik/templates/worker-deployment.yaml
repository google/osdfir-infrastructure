{{- range .Values.workers }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $.Release.Name }}-{{ .name }}
  labels:
    app.kubernetes.io/name: openrelik
    app.kubernetes.io/component: worker
spec:
  replicas: {{ .replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: openrelik
      app.kubernetes.io/component: worker
  template:
    metadata:
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: "false"
      labels:
        app.kubernetes.io/name: openrelik
        app.kubernetes.io/component: worker
        {{- if and .privileged $.Values.gcp.enabled $.Values.config.initWorkerNbd.enabled }}
        cloud.google.com/matching-allowlist: {{ $.Values.config.gkeAllowList }}
        {{- end }}
    spec:
      {{- if and .privileged $.Values.config.initWorkerNbd.enabled }}
      initContainers:
      {{- include "openrelik.initContainerWorker" $ | nindent 8 }}
      {{- end }}
      hostNetwork: false
      hostIPC: false
      automountServiceAccountToken: false
      {{- if $.Values.config.enableServiceAccount }}
      serviceAccountName: openrelik
      {{- end }}
      terminationGracePeriodSeconds: {{ $.Values.config.terminationGracePeriodSeconds }}
      containers:
        - name: worker
          image: {{ .image | quote }}
          imagePullPolicy: {{ $.Values.config.imagePullPolicy }}
          command: ["/bin/sh", "-c"]
          args: ["exec {{ .command }}"]
          terminationMessagePath: /tmp/termination-log
          securityContext:
            privileged: {{ .privileged }}
          env:
          {{- if eq .name "openrelik-worker-timesketch" }}
          {{- include "timesketch.envVariables" $ | nindent 12 }}
          {{- end }}
          {{- if .env }}
            {{- toYaml .env | nindent 12 }} 
          {{- end }}
          {{- if not .privileged }}
            - name: UNPRIVILEGED
              value: "true"
          {{- end }}
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                    name: {{ $.Release.Name }}-openrelik-secret 
                    key: redis-url
            - name: NODENAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          volumeMounts:
            - mountPath: /mnt/openrelikvolume
              name: openrelikvolume
            {{- if .privileged }}
            - mountPath: /dev
              name: dev
              readOnly: true
            {{- end }}
          {{- if .resources }}
          resources:
            {{- toYaml .resources | nindent 12 }}
          {{- end }}
      volumes:
        - name: openrelikvolume
          persistentVolumeClaim:
            {{- if $.Values.persistence.existingPVC }}
            claimName: {{ $.Values.persistence.existingPVC }}
            {{- else }}
            claimName: {{ $.Release.Name }}-openrelikvolume-claim
            {{- end }}
            readOnly: false
        {{- if .privileged }}
        - name: dev
          hostPath:
            path: /dev
        {{- if $.Values.config.initWorkerNbd.enabled }}
        - name: modules
          hostPath:
            path: /lib/modules
        {{- end }}
        {{- end }}
      nodeSelector:
        {{- range $key, $value := .nodeSelector }}
        {{ $key }}: "{{ $value }}"
        {{- end }}
{{- end }}